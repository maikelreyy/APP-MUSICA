//porque hay que poñer o parentesis e as chaves? porque tamos traendo o error do hook useTopAlbums?

const errorMessage = ({ error }) => {
  return <p className="errorMessage">{error}</p>;
};

export default errorMessage;
